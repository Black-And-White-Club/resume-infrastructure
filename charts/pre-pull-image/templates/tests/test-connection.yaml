apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pre-pull-image.fullname" . }}-test-connection"
  labels:
    {{- include "pre-pull-image.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pre-pull-image.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
