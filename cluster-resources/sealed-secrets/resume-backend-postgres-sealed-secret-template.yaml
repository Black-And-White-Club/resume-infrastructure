apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: resume-backend-postgresql
  namespace: resume-db
spec:
  # DEPRECATED: moved to infra - use the canonical secret under all-infrastructure/cluster-resources
  encryptedData:
    # Replace with kubeseal generated keys. Use the following command to create this file:
    # kubectl -n resume-db create secret generic resume-backend-postgresql \
    #   --from-literal=postgresql-password='REPLACE_ME' \
    #   --from-literal=postgresql-username='resume_user' \
    #   --from-literal=postgresql-database='resume_db' \
    #   --dry-run=client -o yaml | kubeseal --format=yaml > sealed-resume-backend-postgresql.yaml
    # NOTE: Do not commit plaintext secrets. Add this sealed secret YAML file to the repo instead.
    # example: postgresq-password: AgB... (kubeseal output)

  template:
    metadata:
      name: resume-backend-postgresql
      labels:
        app: resume-backend
  # See: all-infrastructure/cluster-resources/sealed-secrets/resume-backend-postgres-sealed-secret-template.yaml
