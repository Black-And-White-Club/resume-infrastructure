apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: resume-backend-postgresql
  namespace: resume-app
spec:
  # DEPRECATED: This SealedSecret template was moved to the infra repository.
  # The canonical file is at:
  # https://github.com/Black-And-White-Club/all-infrastructure/blob/main/cluster-resources/sealed-secrets/resume-backend-postgres-sealed-secret-template.yaml
  # Please create and seal the secret there instead of committing an updated secret here.
  encryptedData:
    # Replace this section with `kubeseal` generated sealed secret values.
    # Example local workflow:
    # kubectl -n resume-db get secret resume-backend-postgresql -o yaml > tmp.yaml
    # kubeseal --format=yaml --cert /path/to/sealer-cert.pem < tmp.yaml > sealed-resume-backend-postgresql.yaml
    # commit sealed-resume-backend-postgresql.yaml into the repo
  template:
    metadata:
      name: resume-backend-postgresql
      labels:
        app: resume-backend
  # Example keys to set in the sealed secret:
  #   postgresql-password: <password>
  #   postgresql-username: resume_user
  #   postgresql-database: resume_db
  #   DATABASE_URL: postgresql://resume_user:<password>@resume-backend-postgresql.resume-db.svc.cluster.local:5432/resume_db
